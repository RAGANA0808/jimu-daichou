# 2026-04-22

## 今日やったこと

- プロジェクトの現状を棚卸し（ドキュメント・スキーマは整備済み、実装は未着手）
- **Supabase プロジェクト作成**: `jimu-harness-dev`（東京リージョン、Free プラン）
  - プロジェクトリファレンス: `bfqondhisfsoonxexvrv`
  - 新しい API キー体系（Publishable / Secret）を採用
- **`.env` セットアップ完了**: DATABASE_URL / DIRECT_URL / Supabase URL + 2 キー / SESSION_SECRET（自動生成）
- **依存関係インストール完了**（`pnpm install`）
  - Prisma Client が `apps/web` にしか入っていなかったのでルートの devDependency にも `@prisma/client` を追加
- **初回マイグレーション適用**（`20260422072551_init`）: 全 9 テーブル（Tenant/User/Household/Person/DeathLedgerEntry/MemorialService/GravePlot/Transaction/InteractionNote/Document）が Supabase に作成された
- **seed 実行 → 自寺テナント作成**: `芳全寺 (houzenji)`、ID: `a7c71b60-0fab-4cdf-9c66-0f3f7ae3e38e`
  - seed.ts を改善: プレースホルダ `my-temple` → 本名への自動改名、名前変更を update 句に含めて idempotent 化
- **ハーネス改善 #1 導入**: 作業ログ（`docs/worklog/`）＋ SessionStart フックで自動読み込み
  - `.claude/settings.json` に SessionStart フックを新規追加（`ls -t docs/worklog/[0-9]*.md | head -1` で最新ログを cat）
  - `docs/worklog/README.md` に運用ルールを明記
  - CLAUDE.md §10 に「Claude は節目で worklog を能動的に更新すること」を追記
  - `.claude/settings.json` は git コミット対象（チーム共有）。個人権限は `settings.local.json`（gitignore）

## 決めたこと・気づき

- **Prisma スキーマは初期マイグレーションを通すのに十分な出来**だった。レビューで挙がった改善点（InteractionNote の authorId リレーション未定義、Transaction ↔ MemorialService のリレーション未設定、案内状 NoticeLetter モデル未定義、塔婆の別モデル化検討、Person.birthDate の和暦フィールド）は**いずれも後追い可能**なのでマイグレーション優先で進めた
- **ハーネスエンジニアリング方針**: フォルダ名が `寺務Harness` である通り、CLAUDE.md・フック・スラッシュコマンド等でプロジェクト専用に調律していく。節目で能動的に改善提案する運用（memory に保存済み）
- **新しい Supabase API キー体系**を採用（Legacy の anon/service_role ではなく Publishable/Secret）。環境変数名は既存の `NEXT_PUBLIC_SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` のまま使用（値は新形式）

## 未解決・保留事項

- **RLS（Row Level Security）がまだ有効化されていない**。CLAUDE.md §4.1 で Day 1 から ON が必須とされている。Prisma migrate は自動ではやってくれないので、**別途 SQL マイグレーションで各テーブルに RLS ポリシーを追加する必要**あり。Server Actions で DB を触り始める前に必ず対応
- Prisma 5.22.0 → 7.7.0 のメジャーアップデート通知が出ているが、今は保留（動作優先）
- Next.js 15.0.0 と React 19.0.0 の peer dependency 警告が出ている（実害なし、次回の依存更新タイミングで整理）
- `INITIAL_ADMIN_EMAIL` は `.env` に設定済みだが、まだ User テーブルには投入していない（認証実装時に対応）

## 次回やること

**推奨順**:

1. **ハーネス改善 #2 の提案タイミングを見る**: Server Actions で DB を触り始める直前に `/tenant-check` スラッシュコマンド導入を検討
2. **`lib/wareki/` と `lib/nenki/` の純粋ロジック実装 + テスト** ← UI 非依存で確実に進められる。まずはここから
   - `lib/wareki/`: 西暦 ⇔ 和暦変換（令和・平成・昭和・大正・明治）
   - `lib/nenki/`: 命日起点の年忌計算（1, 3, 7, 13, 17, 23, 27, 33, 37, 50 回忌）
   - Vitest か Node:test でテストを書く（テストランナー未導入なので最初のセットアップが必要）
3. **RLS 有効化の SQL マイグレーション**（`pnpm db:migrate:dev --name enable_rls`）
4. **`lib/db/withTenant()` ラッパー実装** ← CLAUDE.md §4.1 の二重チェック層
5. Supabase Auth + 最初の画面（ダッシュボード or ログイン）
6. 檀信徒カルテ or 過去帳の最初の CRUD

## 参考ログ

- マイグレーションファイル: `prisma/migrations/20260422072551_init/migration.sql`
- Supabase ダッシュボード: https://supabase.com/dashboard/project/bfqondhisfsoonxexvrv
