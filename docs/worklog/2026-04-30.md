# 2026-04-30

## 今日やったこと

### Supabase の "Table publicly accessible" 警告対応

- **発端**: Supabase から「RLS 未有効のテーブルが 1 件あり」のメール警告 (jimu-harness-dev プロジェクト宛)
- **原因特定**: 診断スクリプト `scripts/check-rls.ts` を作って public スキーマの全テーブルの RLS 状態をダンプ → `_prisma_migrations` (Prisma 自動生成のマイグレーション履歴管理テーブル) のみ RLS 未有効と判明。アプリ側の 11 テーブルは全て有効
- **対応**: `prisma/migrations/20260430000000_enable_rls_prisma_migrations/migration.sql` を追加、`pnpm db:migrate` で適用
  - `ALTER TABLE IF EXISTS "_prisma_migrations" ENABLE ROW LEVEL SECURITY;` のみ
  - ポリシーは作らない = anon/authenticated は全拒否、テーブルオーナー (postgres) はバイパス
  - 既存の `20260423022852_enable_rls` と同じ慣習 (FORCE は付けない)
- **検証**: 再度 `check-rls.ts` を実行 → 12 テーブル全て `[OK]`、未有効 0 件

## 決めたこと・気づき

- **`_prisma_migrations` を見落としがちな理由**: Prisma 管理のシステムテーブルなので Prisma スキーマには現れない → 自前の RLS マイグレーション台帳からも漏れる。今回のように「全テーブルを直接 SQL で確認するスクリプト」を 1 本持っておくのが安全
- **実害の評価**: このテーブルに入っているのはマイグレーションファイル名・チェックサム・適用日時のみ。檀信徒データ漏洩には直結しないが、改ざんで Prisma の整合性が壊れる経路になるので Phase 2 (SaaS 化) 前には必修事項
- **`scripts/check-rls.ts` は残す**: 今後新しいテーブル追加時の検査に流用できる。`pnpm tenant-check` の RLS 版として位置づけ

### git 初回コミット (1fd8336)

- **長らく非 git 状態だった寺務台帳を git 管理下に**
  - GitHub の noreply メール (`181286111+RAGANA0808@users.noreply.github.com`) を使用 → 将来 public 化されても本名メールは漏れない
  - `git config` はリポジトリスコープのみ (グローバル設定は変更せず、他プロジェクトと混ざらないように)
  - `.gitignore` に `.claude/settings.local.json` を追加 (ユーザー個別設定はコミットしない)
  - 158 ファイル / 18,559 行を一括コミット

### 会計 (Transaction) CRUD (9b4fed0) — Phase 1 CRUD ブロック完走

- **features/kaikei** 一式
  - `types.ts`: 区分 (`INCOME`/`EXPENSE`) ・カテゴリ (`MAINTENANCE_FEE`/`OFFERING`/`DONATION`/`EVENT_FEE`/`EXPENSE`/`OTHER`) のラベルと並び順、`CATEGORY_BY_DIRECTION` で方向ごとの自然なカテゴリ候補
  - `queries.ts`: `listTransactionsByMonth(year, month?)` / `listTransactionsByHousehold` / `getTransactionById` / `summarizeTransactions` (純関数)
  - `actions.ts`: `createTransactionAction` / `updateTransactionAction` (世帯紐付けは任意・寺側の経費は null)
  - `TransactionForm.tsx`: 区分ラジオで動的にカテゴリ候補を切替 (`useState` + `key={category-${direction}}` で select を再マウント)
- **`/kaikei` ページ群**: 一覧 (月選択ナビ + 収入/支出/差引のサマリーカード + カテゴリ別内訳) / 新規 / 詳細 / 編集
- **`/danshintoto/[id]`**: 入出金履歴セクションを追加 (世帯詳細から `+ 入出金を登録` で `/kaikei/new?householdId=...` に遷移)
- **ダッシュボード**: 「会計」カード追加 (4 → 5 つに)
- **検査結果**: typecheck pass / test 65/65 pass / tenant-check 違反 0

## 決めたこと・気づき

- **`_prisma_migrations` を見落としがちな理由**: Prisma 管理のシステムテーブルなので Prisma スキーマには現れない → 自前の RLS マイグレーション台帳からも漏れる。今回のように「全テーブルを直接 SQL で確認するスクリプト」を 1 本持っておくのが安全
- **実害の評価**: このテーブルに入っているのはマイグレーションファイル名・チェックサム・適用日時のみ。檀信徒データ漏洩には直結しないが、改ざんで Prisma の整合性が壊れる経路になるので Phase 2 (SaaS 化) 前には必修事項
- **`scripts/check-rls.ts` は残す**: 今後新しいテーブル追加時の検査に流用できる。`pnpm tenant-check` の RLS 版として位置づけ
- **会計のスキーマは方向 × カテゴリで重複あり**: 既存スキーマは `direction` (INCOME/EXPENSE) と `category` (MAINTENANCE_FEE...EXPENSE/OTHER) を別カラムで持つが、`category=EXPENSE` は支出方向限定など整合制約はアプリ側 validate で担保。スキーマ変更はせず、`CATEGORY_BY_DIRECTION` 定数で UX 上の整合を取った
- **TypeScript の `noUncheckedIndexedAccess`**: 配列 index アクセス (`array[0]`) が `T | undefined` になるので、フォールバック (`?? 'OTHER'`) を入れて `Array.includes()` の型エラーを回避

## 次回やること

**👉 Phase 1 の残作業 (実装はもうほぼない)**

1. **区画地図の動作確認** (未テスト — 最優先)
   - `pnpm dev` で 3104 起動 → ブラウザで http://localhost:3104
   - エリア作成 → 区画登録 → ドラッグ配置 → パレット戻し → 詳細遷移の一連の流れ
   - 問題があれば `features/kukaku/map/*` を調整
2. **会計 CRUD の動作確認** (未テスト)
   - 月切替・カテゴリ別集計・世帯紐付け切替の挙動確認
   - 世帯詳細から「+ 入出金を登録」で世帯固定の遷移ができるか
3. **Google Cloud Console で OAuth リダイレクト URI に 3104 を追加** (Calendar 連携を使うなら)
4. **GitHub に push** (リモートリポジトリを作成して push、Vercel 連携への導線)
5. **本番デプロイ準備**: Vercel + 本番 URL 切替、Supabase の site URL / Google OAuth redirect URI に本番ドメインを追加

## 未解決・保留事項

- Supabase Advisor のメール警告は再スキャンタイミング (数時間〜1 日) まで残る。ダッシュボードの Advisors から手動再スキャン可
- 区画地図の動作確認 / 会計 CRUD の動作確認はまだ
- 前日からの持ち越し (Prisma 7 / Next.js 15 + React 19 警告 / UUID URL / Codex review P1/P2) はそのまま
- `scripts/check-rls.ts` を `pnpm rls-check` 等の npm script として登録するか検討
