# 2026-06-05

## 今日やったこと

### 開発再開 — Dynamic Workflows による要件網羅＆継続開発へ

- 約1.5ヶ月ぶりに開発再開。方針: **Dynamic Workflows（Claude Code のマルチエージェント・オーケストレーション）を使って、不足機能要件を網羅的に洗い出しつつ既存コードを継続・拡張**する（計画書: `~/.claude/plans/saas-c-users-reon-harness-ticklish-sedgewick.md`）。
- 目指す姿の参照サービス: せいざん「寺務台帳」 https://jimu-daichou.sei-zan.net

### Phase 0: 環境復旧・起動検証（完了）

- `pnpm install` — 既に最新（lockfile変更なし）
- `pnpm db:generate` — Prisma Client (v5.22.0) 生成 OK
- `pnpm typecheck` — ✅ pass
- `pnpm test` — ✅ 65/65 pass（uuid / wareki / nenki）
- `pnpm tenant-check` — ✅ テナント境界違反なし
- `pnpm dev`（3104）起動 → `/login` が 200 で正常描画（メールのマジックリンク方式ログイン画面）。失敗ネットワークリクエスト 0、コンソールエラー 0。
- Claude_Preview MCP を導入（`.claude/launch.json` 作成、`autoPort:false` で 3104 固定 = OAuth コールバック維持）。
  - ※休眠中の dev 子プロセスが 3104 を掴んでいたため一度 taskkill して解放した。

## 決めたこと・気づき

- **ログイン後画面の実機確認は認証セッション必須**（`prisma/seed.ts` 参照: 認証は Supabase Auth のマジックリンクのみ、パスワードログインなし）。区画地図DnD・会計CRUD 等の click-through 検証は、実際に検証対象の機能ができた段階で、マジックリンク経由ログイン（Gmail 連携）をユーザー承認のうえ実施する方針。
- 環境（.env / Supabase）は生きており、ビルド・起動・テストは全てグリーン。Phase 1（要件網羅 Discovery workflow）に進める状態。

## Discovery workflow 完了（Phase 1）

- Dynamic Workflow（17エージェント・約16分）で既存監査＋Webリサーチ→ギャップ分析→**34エピック**のバックログ生成。
- 出力: `docs/requirements/feature-backlog.md` / `docs/requirements/competitor-analysis.md`
- 主な発見: E02対応履歴(schema定義済みなのにUI未実装＝北極星の核が欠落) / E12-E13(共通UI・UX基盤が未整備、components空) / E21 RBAC未参照 / 競合(sei-zan)の月参り巡回(E30)・バックアップ(E26)が欠落。

## Wave 1-A 基盤の足固め 完了（Phase 2）

Dynamic Workflow（5エージェント逐次・メインツリー）で実装。全品質ゲート グリーン（typecheck / test 79件 / tenant-check 違反0）。

- **E12 共通UIキット**: `components/ui/` に shadcn流(Radix+Tailwind+cva)で Button/Input/Select/Textarea/Card/Table/Dialog/ConfirmDialog/Toast/Badge/Breadcrumb/EmptyState/FormField/PageHeader。loading/error/not-found テンプレ。`lib/utils.ts` の cn()。
- **E13 UX基盤**: globals.css/tailwind.config のデザイントークン刷新（16px基準・line-height1.6・WCAG AA状態色・44pxタップ・focus可視・reduced-motion）。文字サイズ3段階切替(localStorage)。
- **E02 対応履歴タイムライン**: `InteractionNote` に UI/Action/query。schema に deletedAt 追加（マイグレーション `20260605004835_add_interaction_note_deleted_at`）。カルテに時系列表示・作成・編集・除外（論理削除）。
- **E01 横断検索**: ヘッダー常設 SearchBar、`lib/search/`（かな/電話正規化の純関数＋テスト14件）、`/api/search`。世帯(かな/電話)＋過去帳(戒名/俗名)横断。
- **E11 気づきダッシュボード**: 静的リンク集→数値/通知カード（今月の法要・年忌対象・空き区画・最新対応履歴）。`features/dashboard/`。

### UX方針の確定（ユーザーフィードバック）

- **高齢者特化に寄せすぎない**（"らくらくスマホ"化しない）。中心はスマホで使いたい次世代の若い住職候補。アクセシビリティは"下限"として維持しモダンなSaaS水準を目指す。
- **PC/ノートPC と スマホ/タブレット の両方を第一級でサポートするレスポンシブ**（PCはテーブル・複数カラムの情報密度／スマホは縦積み・カード化）。
- Foundation のトークンはこの方向に合致（16px標準・上品な配色・拡大はオプション）と確認。既存一覧のテーブル⇔カード化は今後の各機能移行で徹底する。

## E06 過去帳 横断一覧・検索・復元（完了 / 中断後の再開）

前回このエピックの途中で中断していたため、現状の中途変更を Read で点検→破綻箇所を整えて完成させた。全品質ゲート グリーン（typecheck / vitest 120件 / tenant-check 違反0）。

- **横断一覧** `/kakochou`: 寺全体の故人を命日順 / かな順で一覧。PCは `<Table>`、スマホはカード出し分け（`md:` ブレークポイント）。共通UIキット使用。ヘッダーナビに「過去帳」リンク追加。
- **あいまい検索**: `KakochouListControls`（URLクエリ `?q=&sort=` 状態）＋ `listAllDeathLedgerEntries` が `lib/search` の `normalizeKana` でふりがなを寄せ、戒名・俗名・ふりがなの OR 部分一致。
- **除外済み一覧＋復元** `/kakochou/jogai`: deletedAt 済みエントリを除外日/操作者/理由つきで一覧、`RestoreEntryButton` で論理削除の取り消し（冪等）。詳細画面の除外導線も「除外済みの過去帳から復元できます」へ更新。
- **除外監査**: `softDeleteDeathLedgerEntryAction` が `deletedBy`(操作者 User.id)・`deletedReason`(理由) を記録。スキーマ変更はマイグレーション `20260605012042_kakochou_e06_date_precision_and_delete_audit`。
- **没年月日の柔軟化**: `DateOfDeathPrecision`(FULL/YEAR_MONTH/YEAR/UNKNOWN) と構造化フィールド `deathYear/Month/Day` を追加。`dateOfDeath`(DATE) は nullable 化＋既存行をバックフィル（後方互換）。純関数 `lib/kakochou/death-date.ts`(parse/format/sortKey) ＋テスト。明治以前・年のみ・月日不明を登録可能に。
- **重複警告**: `lib/kakochou/duplicate.ts` で同一世帯・同名(正規化一致)を検出。フォームで警告→`confirmDuplicate=true` で続行（登録は妨げない）。
- **テスト**: `features/kakochou/queries.tenant.test.ts`（withTenant 境界＝解決済み tenantId 経由・deletedAt フィルタ・不正 UUID を DB 到達前に弾く、依存モックで実DB不要）。

### 中断由来の破綻を修復した箇所

- `lib/kakochou/duplicate.ts`: `@/lib/search`(server-only を re-export する index) 越し import で純関数テストが弾かれていた → 葉モジュール `@/lib/search/normalize` から直接 import。
- `dateOfDeath` の nullable 化に未追従だった消費側を修正: `features/nenki/queries.ts`（構造化フィールド利用＋`deathYear: {not:null}` で年不明を除外、`AnniversaryMatch.deathDate` の month/day を nullable 化）、`danshintoto/[id]/page.tsx`（`formatDeathDateSeireki` で精度対応表示）。

### E03 法要案内 発送ワークフロー（規模L・完了）

- **2クリック導線**: 年忌表 `/nenki` の「案内を出す」→ `/hasso/new?year=N`（発送ワークフロー）。弔い上げ済み(E22)・離檀・論理削除は `findAnniversariesForYear` 経由で除外済み。
- **新モデル**: `ShipmentBatch`（発送1回＝誰が・いつ・何を＋差込項目: serviceDate/location/offeringGuide/replyDeadline/bodyNote/targetYear）＋ `ShipmentRecipient`（宛先スナップショット）＋ enum `ShipmentDocumentType`。マイグレーション `20260605030000_add_shipment_models`（RLS ポリシー込み、`migrate diff`→手書き SQL→`migrate deploy` で適用。`migrate dev` は非対話環境で不可のため）。
- **純関数 `lib/shipment/`**: `labels.ts`（A4タックシール 21/12/44面の面付け座標計算＋mm→pt）/ `csv.ts`（RFC4180 宛名CSV）/ `validate.ts`（差込フォーム検証・JST日時パース）。Vitest 32 ケース。
- **帳票**: `features/shipment/pdf/` に AddressLabelPdf（面付け）・EnvelopePdf（長3封筒）・MergedNoticeLetterPdf（日時/場所/お布施/締切 差込）。ルート `app/api/shipment/pdf`（type=notice/label/envelope）/ `app/api/shipment/csv`（BOM付）。**PDF失敗は JSON生表示ではなく 400/404＋日本語メッセージ**、UI は toast でエラー表示。
- **発送記録＋カルテ連携**: `recordShipmentAction` が手動トリガ（送信前確認ステップ必須＝特許回避）。宛先はクライアント値を信用せず対象年からサーバ再抽出。記録時に各世帯 `InteractionNote`(Wave1-A) へ発送履歴を1件連携。発送履歴は `/hasso`（一覧）/ `/hasso/[id]`（詳細）。
- **N+1解消**: `listShipmentCandidatesForYear` で household の postalCode/address を `findMany + in` の1クエリ化。旧 `api/nenki/pdf` の逐次 findUnique も同様に解消。
- UI は共通UIキット（PageHeader/Card/Table/Badge/FormField/EmptyState/Button/Select）。PCテーブル↔スマホカードのレスポンシブ出し分け。ナビに「年忌表」「発送」を追加。
- 検証: `pnpm typecheck` ✅ / Vitest 177/177 ✅ / `pnpm tenant-check` ✅ 違反なし。

## Wave 1-B 事務自動化 完了

- ✅E22弔い上げ → ✅E06過去帳横断 → ✅E04塔婆読上帳 → ✅E05中陰計算 → ✅E03法要案内発送。全品質ゲート グリーン（typecheck / test 177件 / tenant-check 違反0）。
- ※途中でユーザーの割り込み停止に連動してワークフローが停止 → E06から継続ワークフローで再開・完走。

## 方針変更（ユーザー指示）

- 1-C（会計・請求・振替/墓地）は一旦保留。**Phase 2 方向へ移行し、まず E16 CSV インポート/エクスポート（データ移行）を優先**。理由: 既存データを他で管理している寺院/ユーザーのオンボーディング（横展開の入口）。
- サービス名は据え置き（あとで検討）。商用化前にサービス名確定＋弁理士 FTO が前提（特許調査済み・`docs/legal/patent-analysis.md`）。

## 次回やること

1. **E16 CSV データ移行**（着手）: 世帯・過去帳・区画・会計の CSV/Excel インポート（列マッピング・重複検出・プレビュー→確定の2段階）＋ Excel/CSV エクスポート。
2. 認証セッションを用意して Wave 1-A/1-B/E16 の実機 click-through 検証（screenshotツールはHMRで idle 待ちタイムアウト。マジックリンク経由ログイン＝Gmail連携をユーザー承認のうえ実施 or Chrome+9222）。
3. 以降: E17書類添付、1-C（会計・請求・振替/墓地）に戻る等。

## 未解決・保留事項

- ログイン後画面の click-through 実機確認（認証セッション取得方法の確定待ち）。
- 前回からの持ち越し（Prisma 7 / Next.js 15+React19 警告 / UUID URL / Codex review P1/P2 / 本番デプロイ準備）はそのまま。

---

## E16 インポート基盤 + 世帯インポート (実装)

- 汎用 CSV/Excel インポート基盤を新設: `apps/web/src/lib/import/`
  - `parse.ts` (papaparse + xlsx でサーバ側パース。CSV/.xlsx 両対応、全セル文字列化)
  - `mapping.ts` (1行目ヘッダ自動推測 + 手動修正、別名/表記ゆれ吸収、二重割当防止)
  - `evaluate.ts` (行ごと severity 判定・確定対象抽出・バッチ chunk)
  - `types.ts` / `registry.ts` (エンティティ差し替え可能な `EntityImportDef<TRecord>` 登録方式)
  - `entities/household.ts` (世帯=最初の対象。Household + Person 取込。lib/search で かな/電話 重複判定→warning+スキップ)
- 2段階フロー: プレビュー(取込予定/警告/エラー一覧) → 確定(一括登録)。確定のみ withTenant で DB 反映。確定時に既存キーを再評価して二重登録防止。バッチ 100 件分割。
- UI: `features/import/ImportWizard.tsx` (4ステップ wizard、共通UIキット使用、PC/スマホ対応)、`features/import/actions.ts`、`app/(main)/import/(page + [id])`。ナビに「取込」導線追加。
- スキーマ変更なし (既存 Household/Person を再利用)。マイグレーション不要。
- 検証: typecheck グリーン / vitest 205 passed (import 純関数テスト 28 件追加) / tenant-check 違反なし。
- 後続 (過去帳・区画・会計) は `entities/` に定義追加 + registry 登録だけで同基盤に乗る。

## 次回やること (追記)

- 過去帳・区画・会計のインポート定義を `lib/import/entities/` に追加。
- エクスポート (CSV/Excel) 側の実装。
- 認証セッションを用意して /import の実機 click-through 検証 (現状マジックリンク未ログインのため未確認)。

## E16 完了 + コードレビュー結果（Claude 担当：静的ゲート＋レビュー）

- E16 全完了（インポート基盤＋世帯/過去帳/区画/会計、エクスポート）。全品質ゲート グリーン（typecheck / test / tenant-check）。
- **検証分担**: 実機 click-through はユーザーがログインして実施（[[jimu-daichou-saas-project]] の方針）。Claude は静的ゲート＋コードレビューまで。
- レビュー所見:
  - ✅ テナント分離（withTenant＋RLS）・個人情報ログ非出力・論理削除遵守・列定義のインポート/エクスポート共有（往復可能）は良好。
  - ⚠️ レビューで「Date の UTC/JST ミスマッチ（高）」が出たが**誤検知**と確認。`@db.Date` は既存 `lib/kakochou/death-date.ts` も `Date.UTC` で保存し `lib/export/format.ts` が `getUTC*` で読む一貫慣習。E16 の `lib/import/date-cell.ts` も一致＝往復は正しい。**変更しない**（JSTローカルに直すと既存と不整合になり壊れる）。
  - 🔧 行数上限を追加（`lib/import/parse.ts` `MAX_IMPORT_ROWS=50000`、DoS 予防。ファイルサイズ10MB上限に加えて行数でも頭打ち）。
  - 🟡 **保留（Phase2課題）**: CSVエクスポートの数式インジェクション対策（=,+,-,@ 始まりセルの無害化）。往復フィデリティ（負数等）を壊さないため自寺運用の現段階では未適用。Phase2（他寺院提供）前に対応。

### ユーザーに確認いただきたい click-through チェックリスト（/import, /export）
- /import: CSV と .xlsx の両方をアップロード → 列マッピング自動推測と手動修正 → プレビュー（取込予定/警告/エラー行）→ 確定 → 世帯/過去帳/区画/会計に反映。
- 重複・表記ゆれ（かな/電話）警告とスキップ挙動。過去帳の没年月日精度（年のみ・月日不明・明治以前・和暦入力）の取込。
- /export: 世帯/過去帳/区画/会計を CSV・Excel で書き出し → そのまま再インポートして往復が成立するか。CSV が Excel で文字化けしないか（BOM）。
- 別テナントのデータが混ざらないこと（RLS の実動作）。

## Wave UI-1 完了 — 目標サービス動画解析→カルテUI/UX強化（Claude：Dynamic Workflow）

- せいざん「寺務台帳」サービス案内動画（youtu.be/_NE8xIuZ900, 48-90分）を Chrome MCP で11主要画面を静止解析 → `docs/requirements/seizan-uiux-video-analysis.md` に分析＋優先度付き改善バックログを作成。
- 解析P0を Dynamic Workflow `wf_2cc59c82-0c6`（設計並列→実装直列→検証）で実装。全ゲート グリーン（typecheck / test 386件 / tenant-check）。
- **A: 対応履歴タイムライン強化** — `InteractionCategory` enum（契約/法要/葬儀/見学/家族/健康/伝言/予定/お参り/戒名/その他）＋`isPinned` を `InteractionNote` に追加（migration `20260605070000_add_interaction_category_pin`、列追加のみ＝既存RLSポリシー継承）。カテゴリ絞り込みチップ・色チップ・伝言ピン留め（最上部固定＋トグル Server Action `toggleInteractionNotePinAction`）。並びは isPinned desc→occurredAt desc。
- **B: 年忌バッジ** — `features/kakochou/NenkiBadges.tsx`（純粋表示）。`lib/nenki`(allAnniversariesOf)＋`lib/wareki`(seirekiToWareki/formatWareki) で「回忌名＋和暦＋西暦＋丸数字」、JST現在年以降の直近回忌を暖色強調。詳細ページ過去帳テーブルに展開行で差し込み。
- **C: 下部固定アクションバー** — `features/danshintoto/HouseholdActionBar.tsx`。対応記録(アンカースクロール)/法要登録/入出金/家族追加/郵便振替/編集。sticky bottom、スマホ横スクロール・PC右寄せ、44pxタップ領域。
- 特許回避: タイムラインは日付降順固定＋ピン最上部のみ。「更新順ポータル自動並べ替え」は実装せず。
- 次ウェーブ候補（解析P1）: グローバル検索バー常設（lib/search 実装済・UI導線）、自由タグ横断抽出、会計クロス集計ビュー E08（科目×月×年計・4月始まり・セル→明細・CSV）。
- 検証分担は従来どおり: 実機 click-through はユーザーがログインして実施。

### ユーザーに確認いただきたい click-through（檀信徒カルテ詳細）
- 対応履歴: 「対応を記録」で話題カテゴリ選択＋ピン留めチェック → 保存。カテゴリチップで絞り込み（再クリック解除）。各記録のピン留めトグルで最上部固定。
- 過去帳: 各故人行の下に年忌バッジ列（和暦＋西暦＋丸数字）、次の回忌が暖色強調されるか。月日不明・年のみ・不明の表示フォールバック。
- 下部固定バー: スマホ／PC両方で常時表示・各ボタン遷移、「対応を記録」で履歴セクションへスクロール。コンテンツが隠れないか（pb）。

## Wave UI-2 完了 — 自由タグ / 会計クロス集計E08 / 備考あいまい検索（Claude：Dynamic Workflow）

- 動画解析P1由来の3点を Dynamic Workflow `wf_13dadfb8-95c`（設計並列→実装直列A→B→C→検証）で実装。全ゲート グリーン（typecheck / test 401件 / tenant-check）。マイグレーション適用済み。
- **A: 自由タグ横断抽出** — `Tag` + `HouseholdTag`（暗黙M2Mを避け tenantId+RLS付き明示的中間モデル）追加（migration `20260605080000_add_household_tags`）。features/tags/（queries/actions/HouseholdTagEditor/HouseholdTagFilter/tag-colors/tag-types）。世帯詳細でタグ付与/解除＋新規作成、世帯一覧でタグ絞り込み（いずれか/すべて切替, URL searchParams）。色は完全クラス文字列マップでJIT purge回避。既存 listHouseholds は温存し listHouseholdsWithTags を新設。
- **B: 会計クロス集計ビュー E08** — features/kaikei/crosstab.ts（純関数 aggregateCrossTab + 年度/月レンジ算出, Date.UTC/getUTC* 厳守）＋crosstab.test.ts(11件)。app/(main)/kaikei/shukei/page.tsx（年度ナビ既定=現在年度JST, 収入/支出2ブロック+差引純額, 月セル→/kaikei?ym=&dir=&cat= で明細遷移, 科目列sticky, 合計行暖色, スマホ横スクロール）。CSV出力は既存 toCsv/csvToBytes(BOM)再利用。kaikei/page.tsx に ?dir/?cat 受け口＋絞り込みチップ＋集計への双方向導線。
- **C: 横断検索の備考あいまい検索** — searchHouseholds に wantMemo ゲート追加、Household.memo/address への ILIKE（escapeLike+ESCAPE維持）。looksLikeMemo（2文字以上で発火, 1文字暴発防止）。結果に memoMatch:boolean のみ（本文非露出＝個人情報非ログ）。SearchBar placeholder更新＋「メモ・住所一致」バッジ。normalize.test.ts +4件。
- 特許回避: タグ/検索とも「更新順ポータル自動並べ替え」は未実装。並びは nameKana 維持。
- 検証分担は従来どおり: 実機 click-through はユーザーがログインして実施。

### ユーザーに確認いただきたい click-through
- タグ: 世帯詳細でタグ作成→付与→解除。世帯一覧でタグチップ絞り込み（いずれか/すべて切替）、URLが共有可能か。
- 会計集計: /kaikei/shukei で年度切替、科目×月×年計・収入/支出/差引、月セルクリックで /kaikei の明細絞り込みへ遷移、CSV出力（Excelで文字化けしないか=BOM）。
- 検索: ヘッダー検索に特徴メモの語（例: 備考に書いた語）を入れて世帯がヒットするか、「メモ・住所一致」バッジ表示。

## Wave UI-3 完了 — 全体ビジュアル刷新（パステルオレンジ系ブランド＋せいざん風ゴシック）（Claude：Dynamic Workflow）

- ユーザー要望: メインカラーを温かいパステルオレンジ/黄系へ（せいざんのティール/シアン回避＝完全パクリ防止）、フォントもせいざん風ゴシック、UX/UIパターンは踏襲。
- Dynamic Workflow `wf_e7e744cb-459`（設計並列→実装直列①トークン基盤②共通UI③掃き出し→検証）。全ゲート グリーン（typecheck / test 401件 / tenant-check）。スキーマ変更なし。
- **トークン刷新（globals.css/tailwind）**: ベース暖色ニュートラル(--background #fdfaf4 等)、ブランド橙 --brand #c2410c(白文字5.18:1 AA)/--brand-hover #9a3208(AAA)/--brand-soft #fdecd9、--accent #9a4a08(AA是正済)、--primary を橙化。状態色は維持しつつ warning を黄土 #8a5a00 にしてブランド橙と弁別。**全『白文字を載せる面』AA(4.5:1)以上を実数確認**。E13基盤(16px/行間1.6/focus2px/min-h-touch/font-scale)は無改変。
- **フォント**: next/font/google で本文=Zen Kaku Gothic New / 見出し=Zen Maru Gothic(font-rounded新設)、display:swap・preload:false、システムゴシックfallback温存。明朝見出し font-serif は全31箇所→font-rounded へ置換（残0）。
- **共通UI（せいざん踏襲）**: table thead=濃橙の塗り帯＋白文字・ゼブラ・hover=brand-soft、button primary=brand橙、badge に brand soft、card/page-header に左ブランド罫＋丸ゴシック見出し、input/select focusボーダー brand。
- **レイアウト**: ヘッダーをブランド橙バー＋白文字ロゴ/ナビに。dark-gray主要ボタンのベタ書きは0件、ティール/シアン誤用も排除（interaction TOUR チップ teal→fuchsia 是正）。
- 手当て: 実装3エージェントが1箇所で不正tool呼び出しにより脱線→未実行だった houyou/new の注意ボックス amber→warning トークン化を親側で手修正、typecheck再グリーン。
- 自由タグの選択式カラー SKY(空色)は、ブランドでなくユーザーが付けるタグ識別用パレットのため意図的に存置。
- 検証分担は従来どおり: 実機ビジュアル目視はユーザーがログインして確認。

### ユーザーに確認いただきたい（実機目視）
- 全体: ヘッダーが橙バー＋白文字、フォントが丸み/ゴシックに、テーブル見出しが濃橙の帯＋白文字、見出しに左の橙罫。
- ダッシュボード/一覧/檀信徒詳細/会計集計などの“ぱっと見”が温かいオレンジ基調に。淡色に白文字で読めない箇所が無いか。
- 文字サイズ切替（ヘッダーの文字サイズ）が従来どおり効くか。

## TaaS Discovery v2 完了 — せいざんスクショ精読→要件網羅＆ロードマップ（Claude：Dynamic Workflow）

- ユーザー提供資料 `G:\…\jimu_daicho_document (1)`（概要HTML全12セクション＋スクショ22枚）を Dynamic Workflow `wf_49c2bf1a-71d`（Ingest並列22枚精読→Audit6ドメイン棚卸し→Synthesize→Critic裏取り）で分析。12エージェント/約105万トークン。
- 成果物3本: docs/requirements/{seizan-coverage-matrix.md（網羅マトリクス＋訂正）, taas-requirements-v2.md（TaaS要件13領域＋非機能/法務）, ui-ux-redesign-v2.md（UI/UX再設計＋実装ロードマップ）}。
- クリティック裏取りで判明: 第2連絡先フォームは実装済(誤判定訂正)/ Document紐付け列不足(要マイグレ)/ refresh_token平文保存(暗号化前倒し)/ 巡回DnDがYMFG特許P8に接近→地図経路描画せず順序リストのみに限定/ RBAC未実効・Burial未実装・カレンダー一方向・戒名順ソート無し・苗字逆引き未統合・Documentアプリ実装ゼロ を確認。
- 推奨ウェーブ順: PERMISSION(RBAC/監査/token暗号化土台) → P0-FOUNDATION(E26 BU/DR＋データ品質KPI) → UI-4(実行中) → GRAVE-CORE(Burial/契約/苗字逆引き) → CONTACT-SEARCH(連絡先構造化/全文検索/承継履歴) → MEMORIAL-EXPIRY → PAPERLESS-MOBILE → OUTREACH-NOTIFY → ANALYTICS-LATER。L系(弁理士FTO/商標)は並行調査。
- 次アクション: Wave UI-4 完了を待って実装ウェーブを ultracode で順次。外部依存(refresh_token暗号鍵/インフラBU設定/対外サービス名・商標)はユーザー判断が要るため明示して切り出す。
