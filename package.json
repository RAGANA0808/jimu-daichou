{
  "name": "jimu-daichou",
  "version": "0.1.0",
  "private": true,
  "description": "寺務台帳 — お寺と檀信徒の関係を 100 年先までつなぐ SaaS",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:migrate": "prisma migrate deploy",
    "db:migrate:dev": "prisma migrate dev",
    "db:generate": "prisma generate",
    "db:seed": "tsx prisma/seed.ts",
    "db:studio": "prisma studio",
    "format": "prisma format",
    "tenant-check": "bash scripts/tenant-check.sh"
  },
  "devDependencies": {
    "@prisma/client": "^5.22.0",
    "@supabase/supabase-js": "^2.104.0",
    "dotenv-cli": "^11.0.0",
    "prisma": "^5.22.0",
    "tsx": "^4.19.0",
    "turbo": "^2.2.0",
    "typescript": "^5.6.0"
  }
}
